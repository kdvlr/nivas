services:
  dashboard:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8000"
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      # Bind mount so all data lives beside this compose file (backup-friendly).
      - ./data:/data
