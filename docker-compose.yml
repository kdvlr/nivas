services:
  dashboard:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8000"
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      - dashboard-data:/data

volumes:
  dashboard-data:
